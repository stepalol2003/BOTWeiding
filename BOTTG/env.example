# Скопируйте в .env на сервере и подставьте свои значения.
# Файл .env не должен попадать в git.

TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_ADMIN_ID=884422112

# Прокси для обхода блокировки Telegram (например, в РФ). Раскомментируйте и укажите свой прокси.
# Без авторизации: PROXY_HTTP=http://proxy.example.com:8080
# С авторизацией — добавьте PROXY_USER и PROXY_PASSWORD.
# PROXY_HTTP=http://host:port
# PROXY_USER=user
# PROXY_PASSWORD=password
